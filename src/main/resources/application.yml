spring:
  datasource:
    url: jdbc:postgresql://192.168.0.102:5432/account
#    url: jdbc:postgresql://localhost:5432/account
    username: postgres
    password: root
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
    properties.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: darshanbehere555@gmail.com
    password:
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
server.port: 8080
server.name: account-server

jwksuri: http://auth/oauth2/jwks